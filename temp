cd "C:\Users\W0024618\Desktop\Trend Analysis"


pwd
ls -la   # or "dir" in PowerShell



git init


# for this repo only
git branch -M main

# or globally (so all new repos use main)
git config --global init.defaultBranch main



git add .gitignore
git commit -m "chore: add .gitignore"




git add .
git status          # review what will be committed
git commit -m "chore: initial import of Trend Analysis project"


git config user.name "Swapnil Diwate"
git config user.email "swapnil.diwate@example.com"





# HTTPS example
git remote add origin https://github.com/<your-gh-username>/trend-analysis.git

# push to set upstream
git push -u origin main






ssh-keygen -t ed25519 -C "Swapnil.Diwate@westernunion.com"
# show key, then copy contents and add to GitHub > Settings > SSH and GPG keys
cat ~/.ssh/id_ed25519.pub





git remote set-url origin git@github.com:<your-gh-username>/trend-analysis.git
git push -u origin main



git status
git branch -a
git remote -v
git log --oneline --decorate --graph -n 10
git config --list        # shows your current git config






